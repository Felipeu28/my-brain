# Teams auth — never commit credentials
scripts/.teams_config.json
scripts/.teams_token.json

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Obsidian workspace (local settings, not content)
.obsidian/workspace.json
.obsidian/workspace-mobile.json

# Logs
scripts/teams_log.txt
*.log

# Quartz build output
node_modules/
.quartz-cache/
public/
.quartz_setup/
.claude/worktrees/
scripts/.teams_token.json
scripts/.teams_config.json
.gstack/

# Private iMessage pulls (local-only, never deployed)
raw/private/
raw/private-*

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
