---
type: pattern-surface
week_ending: 2026-05-02
sources: bookmarks_and_comms=13
tags:
  - graph/leaf
---

# Patterns — Week ending 2026-05-02

## The thesis
This week's bookmarks (@stitchbygoogle Apr 21 *"DESIGN.md open-sourced — agents stop guessing intent"*, @nurijanian Apr 26 *"DESIGN.md: the one file AI needs to match your UI"*, Karpathy's single-CLAUDE.md repo trending #1 on Apr 24) all argue for **per-project machine-readable context files**. Andres is doing exactly that in code: FitLogic PR #4 adds a 173-line `CLAUDE.md` from scratch, Inna CRM commit `258f108` Apr 29 is "Update CLAUDE.md", and the Apr 28 internal call locked in `brand.md` as a per-customer repo artifact for Siren Beauty. The bookmark stream and the commit stream are the same thesis arriving simultaneously.

## Bookmark × code crossover
- **@stitchbygoogle Apr 21 + @nurijanian Apr 26 — DESIGN.md for agents.** Crosses with the **Apr 28 internal-call decision to add `brand.md` to the Siren Beauty repo** ("everything Siren-related stays on-brand"). Same week the pattern enters his bookmarks, he adopts it as repo discipline — that timing is signal, not coincidence.
- **Karpathy's single CLAUDE.md hits 82k stars Apr 24** (@kirillk_web3, @joshmedeski). Crosses with **FitLogic PR #4 adding a 173-line `CLAUDE.md`** and **Inna CRM `258f108` "Update CLAUDE.md" Apr 29**. Two of three active client repos got a CLAUDE.md inside the 7-day window the trending repo dropped.
- **@garrytan Apr 16 — *".txt is code now — llms.txt"*** for agent-discoverable APIs. Crosses with **Business Portal PR #2 (`Moilapp_business`) Apr 30 — real robots.txt + sitemap generator**. Same architectural insight (machine-readable site instructions), different audience (search bots vs LLM agents). The natural next move is llms.txt on moilapp.com — the gap is visible in the bookmark queue.
- **@coreyganim Apr 22 — *"Stitch mockup → client approval → Claude Code"*** workflow. Crosses with **Siren Beauty `109c976` Apr 30 phase-2 "conversion engine"** shipped on top of an approved mockup. Identical 3-step shape.

## What you're about to repeat
The Apr 28 internal call self-diagnosed plainly: *"Right now nothing works because we haven't finished a single project all the way through the finish line."* This week Andres opened **two more parallel builds** (Lunabella PRs #42–44 May 1, Business-plan-Staging Moil360 `referenceMode` enum May 1) on top of four already-mid-flight client systems (Inna, ConnectX, FitLogic, Siren). This has the **same shape as the moilapp.com SEO disaster surfaced Apr 28** — two years of half-shipped pages, 12 of 60 indexed. The multi-CRM "build three then unify into white-label" thesis is structurally the same bet at a different layer. Either one CRM crosses the finish line by May 9, or white-label becomes another `moilapp.com`.

## Anti-patterns reforming
The Apr 30 Heather coaching warning Andres delivered himself — *"a functional-medicine client blew $6K on an online platform that returned ~2 customers"* — is structurally the same as the parallel-CRM build pattern. Four client systems mid-flight, no per-product willingness-to-pay validation beyond initial deposit. The Connectex commits `162ef86` + `3c1ac04` Apr 28 ("restore middleware auth gate" / "restore auth gating") are the operational symptom — restoring something that should never have been broken, on a repo Taiwo hadn't pushed at all going into the Apr 28 walkthrough.

## The question to sit with this week
Of Inna, ConnectX, FitLogic, and Siren Beauty — which one are you marking *done* by Friday, and what does "done" have to mean for the white-label thesis to be one merge away?
